// local dependency
// allows to use a package in current project only
// command : npm i <packagename>
//i  or install both are fine i is a shortone
//we will focus on local dependency

// global dependency
// allows to use a package in anywhere
// command : npm install -g  <packagename>


// package.json ==== manifest file (stores info about the package)
// three ways 
    //1. create package.json file in a root 
    //2 . npm init (step by step , u can skip by enter)
    //3. npm init -y (everythin default)
